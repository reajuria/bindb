{
  "name": "bindb",
  "version": "0.0.1",
  "description": "",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "dev": "tsc && node dist/index.js",
    "start": "node dist/index.js",
    "test": "npm run build && node --test",
    "test:e2e": "npm run build && node --test test-e2e/*.test.js",
    "test:all": "npm run test && npm run test:e2e",
    "benchmark": "npm run build && node benchmarks/scalability.js",
    "benchmark:quick": "npm run build && node benchmarks/index.js --quick",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [],
  "author": "",
  "license": "UNLICENSED",
  "devDependencies": {
    "@types/node": "^24.0.15",
    "typescript": "^5.8.3"
  }
}
